import 'package:flutter/material.dart';

class AtomConnectionButton extends StatelessWidget {
  const AtomConnectionButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}