import 'package:flutter/material.dart';

class MoleculeConnectionButton extends StatelessWidget {
  const MoleculeConnectionButton({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialButton(
        onPressed: () {  },
        
      );
  }
}