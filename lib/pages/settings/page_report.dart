import 'package:flutter/material.dart';

class PageRep extends StatelessWidget {
  const PageRep({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
